'use client';

export default function AdminCurriculumPage() {
  return (
    <div className="space-y-6 text-xs">
      <div className="bg-white p-6 border border-[var(--border)] rounded">
        <h1 className="text-2xl font-bold text-[var(--primary-dark)]">Academic Curriculum Standards</h1>
        <p className="text-xs text-[var(--muted-text)]">Manage national curriculum guidelines, scheme of work documents, and learning objectives.</p>
      </div>
      <div className="bg-white p-5 border border-[var(--border)] rounded font-semibold text-[var(--muted-text)]">
        Curriculum Structure: Nigerian National Curriculum integrated with Montessori early years framework.
      </div>
    </div>
  );
}
